import React from 'react'

export default function SearchBar(props) {
  return (
    <>
    {/* <form>
      <input type="text" placeholder='Enter city or town name' onChange={event => {event.preventDefault();props.changeCity(event.target.value)}}/>
      <button type='submit'>search</button>
    </form> */}
    </>
  )
}
